spring:
  application:
    name: configserver
  profiles:
    active: dev,mysql
  security:
    user:
      name: admin
      password: admin
      roles: ADMIN
server:
  port: 8888

management:
  endpoints:
    web:
      exposure:
        include: "*"

  endpoint:
    health:
      show-details: always
