spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        bootstrap: true
        git:
          uri: file:///Users/denglt/onGithub/springcloud
          username:
          password:
          searchPaths: config-repo
      profile: default
