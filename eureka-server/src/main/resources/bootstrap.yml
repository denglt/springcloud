spring:
  application:
    name: eurekaserver
  cloud:
    config:
      uri: http://127.0.0.1:8888
      username: admin
      password: configserver
      profile: secure
      fail-fast: true
      retry:
        initial-interval: 1000
      label: master

